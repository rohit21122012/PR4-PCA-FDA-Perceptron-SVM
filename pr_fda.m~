function [ reducedX ] = pr_fda( X, Y )
    posX = X(Y==0,:);
    negX = X(Y==1,:);
    scatterX = cov(posX) * size(posX,1);
    sca
    withClassScatter = 
   
    
   reducedX = inv(withClassScatter)*(mean(posX) + mean(negX)) 
end

